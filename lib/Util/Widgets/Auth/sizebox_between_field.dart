import 'package:flutter/material.dart';

class SizeBetField extends StatelessWidget {
  const SizeBetField({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 24,
    );
  }
}
